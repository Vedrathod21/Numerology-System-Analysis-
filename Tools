import React from "react";
import { Link } from "react-router-dom";
import { ArrowLeft } from "lucide-react";
import {
  Tabs,
  TabsList,
  TabsTrigger,
  TabsContent,
} from "../components/Tabs";
import { Button } from "../components/Button";
import styles from "./tools.module.css";

// Tools Components
import { NameChangeTool } from "../components/NameChangeTool";
import { CompatibilityTool } from "../components/CompatibilityTool";
import { OptimizationTool } from "../components/OptimizationTool";
import { LuckyDatesTool } from "../components/LuckyDatesTool";
import { AddressTool } from "../components/AddressTool";
import { ChineseTool } from "../components/ChineseTool";

export default function ToolsPage() {
  return (
    <div className={styles.container}>
      <header className={styles.header}>
        <Button variant="ghost" size="icon-sm" asChild>
          <Link to="/">
            <ArrowLeft size={20} />
          </Link>
        </Button>
        <div>
          <h1 className={styles.title}>Neuro-Numerology Tools</h1>
          <p className={styles.subtitle}>Advanced Analysis Suite</p>
        </div>
      </header>

      <Tabs defaultValue="name-change" className={styles.tabs}>
        <TabsList className={styles.tabsList}>
          <TabsTrigger value="name-change">Name Change</TabsTrigger>
          <TabsTrigger value="compatibility">Compatibility</TabsTrigger>
          <TabsTrigger value="optimization">Optimization</TabsTrigger>
          <TabsTrigger value="lucky-dates">Lucky Dates</TabsTrigger>
          <TabsTrigger value="address">Address</TabsTrigger>
          <TabsTrigger value="chinese">Chinese</TabsTrigger>
        </TabsList>

        <div className={styles.tabContentWrapper}>
          <TabsContent value="name-change">
            <NameChangeTool />
          </TabsContent>
          <TabsContent value="compatibility">
            <CompatibilityTool />
          </TabsContent>
          <TabsContent value="optimization">
            <OptimizationTool />
          </TabsContent>
          <TabsContent value="lucky-dates">
            <LuckyDatesTool />
          </TabsContent>
          <TabsContent value="address">
            <AddressTool />
          </TabsContent>
          <TabsContent value="chinese">
            <ChineseTool />
          </TabsContent>
        </div>
      </Tabs>
    </div>
  );
}
